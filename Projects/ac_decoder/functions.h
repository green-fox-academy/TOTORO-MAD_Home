#include <stdint.h>

#define LOW_TIME                326
#define HIGH_TIME_ZERO          408
#define HIGH_TIME_ONE           982

uint16_t correction(uint16_t diff);

char filling_binary(uint16_t diff);
